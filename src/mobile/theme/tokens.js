// Re-export from the canonical token file so all imports resolve correctly.
// All mobile files use: import { ... } from '../theme/tokens'
export { colors, radius, spacing, typography, shadow } from '../data/token.js';
