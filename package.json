{
  "name": "poolseye",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "mobile": "npm start --prefix mobile",
    "install:all": "npm install --prefix frontend && npm install --prefix mobile"
  }
}
